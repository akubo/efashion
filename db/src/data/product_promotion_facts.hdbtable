COLUMN TABLE "efashion.db.data::PRODUCT_PROMOTION_FACTS" (
  PRODUCT_PROMOTION_FACTS_ID INTEGER NOT NULL PRIMARY KEY,
  ARTICLE_ID INTEGER,
  WEEK_ID INTEGER,
  PROMOTION_ID INTEGER,
  DURATION INTEGER,
  PROMOTION_COST INTEGER
);
